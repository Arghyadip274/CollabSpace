{
  "name": "collabspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/frontend",
    "dev:frontend": "npm run dev --workspace=apps/frontend",
    "build:frontend": "npm run build --workspace=apps/frontend",
    "typecheck": "npm run typecheck --workspace=apps/frontend --workspace=packages/shared"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
